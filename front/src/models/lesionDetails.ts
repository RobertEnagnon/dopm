type LesionDetails = {
    /**Dois correspondre à la future table */
    id: number,
    name?: string,
    createdAt?: Date | string,
    updatedAt?: Date | string
}

export type {
    LesionDetails
}